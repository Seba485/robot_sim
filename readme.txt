in order to start the simulation

    roslaunch robot_model_pkg room.launch

in order to start the wasd keyboard controller

    roslaunch robot_controller wasd_controller.launch

in order to access the output of traversability node

    roslaunch hmm_sim only_traversability.launch